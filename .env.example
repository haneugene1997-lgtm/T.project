# Vercel → Settings → Environment Variables 에 동일하게 추가
# Gemini API Key
GEMINI_API_KEY=your-gemini-api-key-here

# 기본 모델 (텍스트 질문 등). AI Studio에서 한도가 있는 ID로 맞출 것
GEMINI_MODEL=gemini-2.5-flash

# 파일 첨부 시 서버가 먼저 시도할 모델 (선택). 비우면 Flash Lite → Flash 순으로 자동 시도
# GEMINI_MODEL_FILES=gemini-2.5-flash-lite
